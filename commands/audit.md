---
description: Lint CLAUDE.md and context files — contradictions, stale references, bloat, uncheckable rules
argument-hint: "[paths… | --global]"
---

Use the Skill tool to invoke `prompt-coach:prompt-audit` with these arguments: $ARGUMENTS

If no arguments were given, audit the default set: ./CLAUDE.md, ./CLAUDE.local.md, and .claude/*.md in the current project.
