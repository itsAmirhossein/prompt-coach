---
description: Analyze and improve a prompt — lint findings, context suggestions, two rewrite candidates
argument-hint: "[prompt text | --last | --file <path>] [--mode swe|research|writing] [--target <model>] [--deep] [--test]"
---

Use the Skill tool to invoke `prompt-coach:prompt-improve` with these arguments: $ARGUMENTS

If no arguments were given, first ask whether to analyze the previous prompt in this session (`--last`) or a prompt the user will paste.
