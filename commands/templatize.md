---
description: Turn a prompt into a reusable {{variable}} template, induce one from examples, or use a saved template
argument-hint: "[prompt text | --last | --file <path> | --from-examples | --list | --use <name>]"
---

Use the Skill tool to invoke `prompt-coach:prompt-templatize` with these arguments: $ARGUMENTS

If no arguments were given, list the saved templates (if any) and ask what to templatize.
