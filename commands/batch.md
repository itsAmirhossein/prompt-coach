---
description: Batch and file input — analyze many prompts or a file of prompts/conversations, then summarize recurring issues
argument-hint: "[prompt text… | --file <path> | path] [--mode swe|research|writing] [--as prompts|conversations|context] [--deep]"
---

Use the Skill tool to invoke `prompt-coach:prompt-batch` with these arguments: $ARGUMENTS

If no arguments were given, ask the user to paste the prompts/conversations to review, or to give a `--file <path>`.
